{
  "name": "universal-systems-service-app",
  "version": "1.0.0",
  "private": true,
  "description": "Universal Systems service and repair management web application",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "install:all": "npm install --prefix server && npm install --prefix client",
    "build": "npm run build --prefix client",
    "seed:demo": "npm --prefix server run seed:demo",
    "seed:live": "npm --prefix server run seed:live",
    "db:clean-client": "npm --prefix server run db:clean-client --",
    "start": "npm run start --prefix server"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
